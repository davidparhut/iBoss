# iBoss - Преміальні iPhone в Львові

Професійний React-додаток для продажу та ремонту iPhone з інтеграцією Firebase.


## 📦 Структура проєкту

```
iboss-app/
├── src/
│   ├── components/
│   │   ├── Navigation.jsx    # Навігація з авторизацією
│   │   └── Footer.jsx         # Футер сайту
│   ├── pages/
│   │   ├── HomePage.jsx       # Головна сторінка
│   │   ├── ProductsPage.jsx   # Каталог iPhone (Firebase)
│   │   ├── RepairPage.jsx     # Сторінка ремонту (Firebase)
│   │   └── LoginPage.jsx      # Авторизація (Firebase Auth)
│   ├── firebase/
│   │   ├── config.js          # Конфігурація Firebase
│   │   ├── auth.js            # Функції авторизації
│   │   └── firestore.js       # Робота з БД
│   ├── context/
│   │   └── AuthContext.jsx    # Контекст авторизації
│   ├── styles/
│   │   └── App.css            # Всі стилі
│   ├── App.jsx                # Головний компонент + роутинг
│   └── index.jsx              # Точка входу
├── index.html
├── package.json               # Залежності (+ Firebase)
├── vite.config.js            # Конфігурація Vite
├── README.md                 # Ця інструкція
├── FIREBASE_GUIDE.md         # Детальний гайд по Firebase
└── ROUTING_GUIDE.md          # Гайд по роутингу
```


### Використання:
```javascript
import { useAuth } from './context/AuthContext';

function MyComponent() {
  const { currentUser, isAuthenticated } = useAuth();
  // ...
}
```

## 📊 База даних Firestore

### Колекції:
1. **products** - iPhone та аксесуари
2. **repairServices** - Послуги ремонту
3. **orders** - Замовлення користувачів
4. **repairRequests** - Заявки на ремонт
5. **carts** - Кошики покупок

### Fallback режим:
Якщо в Firestore немає даних, додаток автоматично використовує дефолтні дані.



## 🔧 Технології

- React 18
- React Router DOM 6
- Firebase 10.7.1
  - Authentication
  - Firestore Database
  - Analytics
- Vite
- CSS3
- Google Fonts (Libre Baskerville, DM Sans)
